# Web framework
fastapi==0.141.1
uvicorn[standard]==0.52.0
python-multipart==0.0.9
websockets==16.0

# LangGraph + LangChain — 1.x ecosystem
langgraph==1.2.2
langchain==1.3.2
langchain-core==1.4.5
langchain-openai==1.3.0
langchain-groq==1.1.3
langchain-aws>=0.2.0,<2.0.0
langchain-community==0.4.2
langsmith==0.8.6

# AWS SDK (Bedrock runtime)
boto3>=1.35.0

# OpenAI
openai==2.41.1

# STT — Deepgram
deepgram-sdk==3.7.3

# TTS — ElevenLabs
elevenlabs==2.60.0

# VAD — barge-in detection
webrtcvad-wheels==2.0.14        # pre-built wheels, no compiler needed

# Redis — session state
redis[hiredis]==8.1.0

# PostgreSQL checkpoint (LangGraph persistent memory)
psycopg[async,binary]>=3.3.4
langgraph-checkpoint-postgres==3.1.1

# RAG — vector store
pgvector==0.5.0
psycopg2-binary==2.9.9
sqlalchemy==2.0.48

# Twilio helper
twilio==9.2.3

# Audio processing
numpy==1.26.4
# audioop is built-in on Python 3.12 (removed in 3.13 only)

# HTTP client
aiohttp==3.13.5
httpx==0.28.1

# Config
pydantic-settings==2.13.1
python-dotenv==1.0.1

# Utils
structlog==25.5.0

# Testing / dev
fakeredis==2.37.0         # in-memory Redis drop-in for unit tests
