FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn[standard] structlog

COPY mock_cno_api.py .

EXPOSE 8001
CMD ["uvicorn", "mock_cno_api:app", "--host", "0.0.0.0", "--port", "8001"]
